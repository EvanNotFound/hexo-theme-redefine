## 1. Update Article Title Scale

- [x] 1.1 Set regular and cover article title utilities to `text-3xl md:text-4xl`, preserving all other classes.

## 2. Reduce Markdown Heading Defaults

- [x] 2.1 Update the desktop and tablet `h1`-`h6` font sizes in `source/css/common/markdown.styl` while preserving spacing, weights, borders, letter spacing, and line heights.

## 3. Verify Typography Build

- [x] 3.1 Run the theme build and verify both title variants and all Markdown heading defaults match the specification.
