---
title: 插入自定义代码
description: 配置项 `inject`
---

**配置项名称：`inject`**

本功能能让你在页面中插入自定义的代码，比如统计代码、点击样式等各种 CSS/JS 代码。

## 配置

在 Redefine 主题配置文件 `_config.redefine.yml` 的 `inject` 配置项中，你可以配置以下内容：

* `enable`：是否启用本功能，`true` 为启用，`false` 为禁用。
* `head`：插入到页面头部的代码，比如统计代码等。
* `footer`：插入到页面底部的代码，比如点击样式等。
